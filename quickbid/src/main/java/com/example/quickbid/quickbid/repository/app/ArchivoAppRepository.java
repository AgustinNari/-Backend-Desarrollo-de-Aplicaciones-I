package com.example.quickbid.quickbid.repository.app;

import org.springframework.data.jpa.repository.JpaRepository;

import com.example.quickbid.quickbid.entity.app.ArchivoApp;

public interface ArchivoAppRepository extends JpaRepository<ArchivoApp, Long> {
}
